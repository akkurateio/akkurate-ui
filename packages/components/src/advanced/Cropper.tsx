import { Box, Text } from "@chakra-ui/react"
import React, { FunctionComponent } from "react"

interface IProps {}

const Cropper: FunctionComponent<IProps> = ({}) => {
  return (
    <Box>
      <Text>Cropper</Text>
    </Box>
  )
}

export default Cropper
